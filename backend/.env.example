# Database
DATABASE_URL="sqlite:///./trustverse.db"

# Security
SECRET_KEY="replace_this_with_a_secure_random_string"
ALGORITHM="HS256"

# Blockchain (local Hardhat node by default; overwritten by scripts/dev.sh after deploy)
ETH_RPC_URL="http://127.0.0.1:8545"
ISSUER_REGISTRY_ADDRESS="0x..."
ANCHOR_CONTRACT_ADDRESS="0x..."
REVOCATION_CONTRACT_ADDRESS="0x..."
VERIFICATION_GATEWAY_ADDRESS="0x..."
GROTH16_VERIFIER_ADDRESS="0x..."

# Pinata / IPFS (Optional for local testing; falls back to mock CIDs if unset)
PINATA_API_KEY=""
PINATA_SECRET_API_KEY=""
